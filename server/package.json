{
  "name": "genbionex-server",
  "version": "1.0.0",
  "private": true,
  "description": "Contact-form endpoint for the GenBionex website. Serves the static site and POST /api/contact.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  },
  "engines": {
    "node": ">=18"
  }
}
